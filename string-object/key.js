const pc ={
    brand : 'Megabite',
    monitor : ' MSI ',
    cpu :'Tharmal Tech',
    madarboard : 'Megabite',
    ssd : '256GB',
}
// console.log(pc);
const keys = Object.keys(pc);
console.log(keys);
for ( const key of keys){
    console.log(key);
}