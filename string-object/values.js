const pc ={
    brand : 'Megabite',
    monitor : ' MSI ',
    cpu :'Tharmal Tech',
    madarboard : 'Megabite',
    ssd : '256GB',
}
console.log(pc);
const values = Object.values(pc);
console.log(values);
for(const value of values){
    console.log(value)
}
